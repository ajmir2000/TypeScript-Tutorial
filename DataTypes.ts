const isProgrammingFun: boolean = true;

const total: number = 42;

const name: string = "Ajmir";

const numbers: number[] = [1, 2, 3, 4, 5];

const person: { name: string; age: number } = {
  name: "Ajmir",
  age: 25,
};

const cities: string[] = ["New York", "London", "Tokyo"];

const classes: Array<string> = ["Math", "Science", "History"];

const tuple: [string, number] = ["Ajmir", 25];

let randomValue: any = 10;
randomValue = "Now I'm a string";
